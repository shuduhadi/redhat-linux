# Command Line Assistant Commands

## Ask the AI Assistant

Ask RHEL Lightspeed a question from the terminal.

```bash
c chat "Why should I register RHEL?"
```

Replace the question with your own.

Example:

```bash
c chat "How do I create a new user?"
```